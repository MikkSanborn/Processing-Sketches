class ScreenDisplayable implements IsDisplayable {
  // Home, level select
  // Level (subclass) holds individual levels.
  
  
  public ScreenDisplayable() {
  }
  
  public void display() {
  }
}
